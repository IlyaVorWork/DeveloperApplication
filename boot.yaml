---
gin:
  - name: developer_application
    port: 8080
    enabled: true
    sw:
      enabled: true
postgres:
  - name: developer_application_postgres
    enabled: true
    addr: "${POSTGRES_ADDR}"
    user: "${POSTGRES_USER}"
    pass: "${POSTGRES_PASS}"
    database:
      - name: "${POSTGRES_DB}"
        autoCreate: true
